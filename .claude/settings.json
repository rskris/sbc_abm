{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pip install -q -e '.[dev,geo,osm]' 2>/dev/null || pip install -q -e '.[dev]' 2>/dev/null || pip install -q numpy pandas pyyaml requests networkx pytest 2>/dev/null; true"
          }
        ]
      }
    ]
  }
}
